# {项目名} — 无缝衔接（{更新日期} 更新）

> 新对话第一句：**请先阅读本文件全部内容。**
> 工作目录：`{项目绝对路径}`

---

## 本轮核心改动摘要（{日期范围}）

{改动摘要章节 — 增量更新时保留旧章节，在末尾追加新章节，编号递增}

---

## 服务架构拓扑

### 线上链路

{域名 → CDN → 代理 → 源站 → 数据库，如未知则标注"待补充"}

### 本地链路

{开发环境服务列表}

### 部署位置标注

| 服务 | 位置 | 说明 |
|------|------|------|
| {服务名} | {VPS/本地/已停用} | |

## 当前服务状态

| 服务 | 地址 | 状态 | 说明 |
|------|------|------|------|
| {从探测结果自动填充} | | running/stopped/unknown | |

## 改过的文件

```
{git diff --name-only 的结果，标注本轮修改的文件}
```

## 当前凭据/平台状态

| 平台/服务 | Token/状态 | 备注 |
|-----------|-----------|------|
| {从知识库或 .env 读取，只显示"已配置/未配置"} | | |

## 关键配置提醒

- {重要配置项}
- {从知识库提取的事故教训}
- ⚠️ 禁止: {根据项目上下文标注的基础设施红线}

## 待办

{合并去重后的待办列表}

## 部署/恢复命令

```bash
# {前端部署命令 — 如未知标注"待补充"}
# {后端部署命令}
# {服务重启命令}
# ⚠️ 禁止: {标注绝对不能运行的命令}
```

## 新对话接手步骤

1. 确认本地服务是否正常
2. 确认线上服务是否正常
3. 读相关文档/SKILL
4. {项目特定的接手步骤 — 从知识库或 AGENTS.md 推断}
